import { AandBandC } from '../aAndBAndC';

export class AdjectiveCoreExpansionServiceProvider extends AandBandC {
    constructor(coreExpansion: string, adjective: string, serviceProvider: string) {
        super(adjective, coreExpansion, serviceProvider);
    }
}
