import { AandBandC } from '../aAndBAndC';

export class AdjectiveCoreJobTitleTransactional extends AandBandC {
    constructor(coreJobTitle: string, adjective: string, transactional: string) {
        super(adjective, coreJobTitle, transactional);
    }
}
