import { AandBandC } from './aAndBAndC';

export class AdjectiveCoreTransactional extends AandBandC {
    constructor(core: string, adjective: string, transactional: string) {
        super(core, adjective, transactional);
    }
}
